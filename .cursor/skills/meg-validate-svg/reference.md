# Naming notes (MEGS-0005)

Compacted duplicates were removed. Shippable catalog is snake_case-only (24 glyphs).

Forbidden basenames (must not reappear):

- `authoritymark.svg`
- `certitudepoint.svg`
- `sarmark.svg` (use `sar_mark.svg`)
- `doublepoint.svg` / `humanjudgement.svg`

Canonical map: `config/unicode-map.json`. Catalog JSON: `docs/glyphs/<id>.json`.
